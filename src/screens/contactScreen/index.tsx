import DefaultLayout from "@/layouts/defaultLayout";
import React from "react";

const ContactScreen = () => {
  return (
    <div>
      <h1>Contact</h1>
    </div>
  );
};

export default DefaultLayout(ContactScreen);
