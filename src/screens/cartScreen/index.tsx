import DefaultLayout from "@/layouts/defaultLayout";
import React from "react";

const CartScreen = () => {
  return (
    <div>
      <h1>Cart</h1>
    </div>
  );
};

export default DefaultLayout(CartScreen);
