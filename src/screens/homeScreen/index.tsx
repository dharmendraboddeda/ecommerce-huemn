import React, { useState } from "react";

import DeaderLayout from "@/layouts/defaultLayout";

function HomeScreen() {
  return (
    <div>
      <h1>Home</h1>
    </div>
  );
}

export default DeaderLayout(HomeScreen);
